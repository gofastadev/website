services:
  web:
    build:
      context: .
      target: deps
    working_dir: /app
    command: yarn dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - NODE_ENV=development

volumes:
  node_modules:
