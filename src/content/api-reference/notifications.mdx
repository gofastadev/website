---
title: Notifications
description: Multi-channel notification dispatch with support for email, database, Slack, push, and custom notification channels.
---

# Notifications

The `notifications` package provides a unified notification dispatch system that supports multiple channels including email, database, Slack, push notifications, and custom channels. It uses the mailer package internally for the email channel. It decouples notification logic from delivery mechanisms.

## Import

```go
import "github.com/gofastadev/gofasta/pkg/notifications"
```

## Key Types

### Notifier

```go
type Notifier interface {
    Send(ctx context.Context, notification Notification) error
    SendTo(ctx context.Context, notification Notification, channels ...string) error
    RegisterChannel(name string, channel Channel)
}
```

### Channel

```go
type Channel interface {
    Deliver(ctx context.Context, recipient Recipient, notification Notification) error
    Name() string
}
```

### Notification

```go
type Notification struct {
    Type    string                 `json:"type"`
    Title   string                 `json:"title"`
    Body    string                 `json:"body"`
    Data    map[string]interface{} `json:"data,omitempty"`
}
```

### Recipient

```go
type Recipient struct {
    ID       string            `json:"id"`
    Email    string            `json:"email,omitempty"`
    DeviceID string            `json:"device_id,omitempty"`
    Meta     map[string]string `json:"meta,omitempty"`
    Channels []string          `json:"channels"`
}
```

## Key Functions

| Function | Signature | Description |
|----------|-----------|-------------|
| `NewNotifier` | `func NewNotifier() Notifier` | Creates a new notifier instance |
| `NewEmailChannel` | `func NewEmailChannel(m mailer.Mailer, templatePath string) Channel` | Creates an email delivery channel (uses the mailer package internally) |
| `NewDatabaseChannel` | `func NewDatabaseChannel(db *gorm.DB) Channel` | Creates a database-backed notification channel |
| `NewSlackChannel` | `func NewSlackChannel(cfg SlackConfig) Channel` | Creates a Slack notification channel |
| `NewPushChannel` | `func NewPushChannel(cfg PushConfig) Channel` | Creates a push notification channel (FCM/APNs) |

## Usage

### Setting Up Notifications

```go
notifier := notifications.NewNotifier()

// Register email channel (uses the mailer package internally)
emailCh := notifications.NewEmailChannel(mailClient, "templates/notifications/")
notifier.RegisterChannel("email", emailCh)

// Register database channel
dbCh := notifications.NewDatabaseChannel(db)
notifier.RegisterChannel("database", dbCh)

// Register Slack channel
slackCh := notifications.NewSlackChannel(notifications.SlackConfig{
    WebhookURL: "https://hooks.slack.com/services/...",
})
notifier.RegisterChannel("slack", slackCh)

// Register push notification channel
pushCh := notifications.NewPushChannel(notifications.PushConfig{
    FCMServerKey: "your-fcm-key",
})
notifier.RegisterChannel("push", pushCh)
```

### Sending a Notification

```go
notification := notifications.Notification{
    Type:  "order_confirmed",
    Title: "Order Confirmed",
    Body:  "Your order #12345 has been confirmed.",
    Data: map[string]interface{}{
        "order_id": "12345",
        "amount":   99.99,
    },
}

recipient := notifications.Recipient{
    ID:       "user-123",
    Email:    "user@example.com",
    DeviceID: "fcm-device-token",
    Channels: []string{"email", "push"},
}

err := notifier.Send(ctx, notification)
```

### Sending to Specific Channels

```go
// Send only via push, skipping email
err := notifier.SendTo(ctx, notification, "push")
```

### Creating a Custom Channel

Implement the `Channel` interface to add custom delivery mechanisms.

```go
type SMSChannel struct {
    client *twilio.Client
}

func (s *SMSChannel) Name() string {
    return "sms"
}

func (s *SMSChannel) Deliver(ctx context.Context, recipient Recipient, n Notification) error {
    phone := recipient.Meta["phone"]
    return s.client.SendSMS(phone, n.Body)
}

// Register it
notifier.RegisterChannel("sms", &SMSChannel{client: twilioClient})
```

### Notification in Service Layer

```go
func (s *OrderService) ConfirmOrder(ctx context.Context, orderID string) error {
    order, err := s.repo.FindByID(ctx, orderID)
    if err != nil {
        return err
    }

    order.Status = "confirmed"
    if err := s.repo.Update(ctx, order); err != nil {
        return err
    }

    return s.notifier.Send(ctx, notifications.Notification{
        Type:  "order_confirmed",
        Title: "Order Confirmed",
        Body:  fmt.Sprintf("Your order #%s has been confirmed.", order.ID),
        Data:  map[string]interface{}{"order_id": order.ID},
    })
}
```

## Related Pages

- [Mailer](/docs/api-reference/mailer) -- Email channel uses the mailer package
- [Queue](/docs/api-reference/queue) -- Queue notifications for async delivery
- [WebSocket](/docs/api-reference/websocket) -- Real-time notifications via WebSocket
