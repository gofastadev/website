---
title: gofasta version
description: Display the CLI version, the Go compiler version it was built with, and the OS/architecture target. Useful for bug reports, CI environment checks, and verifying the right CLI is installed across a team.
keywords:
  - "CLI"
  - "version"
  - "build info"
  - "go version"
  - "CLI version"
  - "gofasta version"
---

# gofasta version

Displays the CLI version, Go version, and OS/architecture details.

## Usage

```bash
gofasta version
```

This command takes no flags. You can also use the `-v` or `--version` flag on the root command:

```bash
gofasta -v
```

## Examples

```bash
$ gofasta version
gofasta version 0.6.0
Go:     go1.25.0
OS:     darwin/arm64
```

## Related

- [gofasta upgrade](/docs/cli-reference/upgrade) -- upgrade to the latest version
- [gofasta doctor](/docs/cli-reference/doctor) -- check system prerequisites
