// Keystatic's client-side router handles every URL under /keystatic/*
// from inside the layout. The page itself renders nothing — the
// layout's SPA mount is the entire surface.
export default function Page() {
  return null;
}
